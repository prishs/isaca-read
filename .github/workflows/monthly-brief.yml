name: Monthly journal brief

on:
  schedule:
    # 1st of the month, 01:18 UTC = 6:48 AM IST
    - cron: "18 1 1 * *"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  brief:
    uses: ./.github/workflows/generate.yml
    with:
      prompt_file: prompts/monthly-brief.md
      output_file: briefs/{{MONTH}}.md
    secrets: inherit

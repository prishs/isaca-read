name: Daily digest

on:
  schedule:
    # 00:18 UTC = 5:48 AM IST (India, no DST) on weekdays
    - cron: "18 0 * * 1-5"
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  digest:
    uses: ./.github/workflows/generate.yml
    with:
      prompt_file: prompts/daily-digest.md
      research_prompt_file: prompts/daily-digest-research.md
      combined_prompt_file: prompts/daily-digest-combined.md
      output_file: digests/{{DATE}}.md
      two_phase: true
    secrets: inherit
